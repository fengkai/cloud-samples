subprojects{
    apply plugin: "java"

    dependencyManagement {
        dependencies{
            dependency "org.apache.dubbo:dubbo:2.7.7"
            dependency "org.apache.dubbo:dubbo-dependencies-zookeeper:2.7.7"
        }
    }

    test {
        useJUnitPlatform()
    }
}